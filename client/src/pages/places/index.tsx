import Head from 'next/head';
import * as React from 'react';

const Places: React.FC = () => {
  return (
    <React.Fragment>
      <Head>
        <title>Places</title>

        <div></div>
      </Head>
    </React.Fragment>
  );
};

export default Places;
