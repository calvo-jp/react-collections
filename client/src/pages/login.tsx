import Head from 'next/head';
import * as React from 'react';

const Login: React.FC = () => {
  return (
    <React.Fragment>
      <Head>
        <title>Login</title>

        <div></div>
      </Head>
    </React.Fragment>
  );
};

export default Login;
