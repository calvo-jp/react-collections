import Head from 'next/head';
import * as React from 'react';

const Landing = () => {
  return (
    <React.Fragment>
      <Head>
        <title>Pedicab</title>
      </Head>
    </React.Fragment>
  );
};

export default Landing;
